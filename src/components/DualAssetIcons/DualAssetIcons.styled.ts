import styled from "styled-components/macro"

export const IconsWrapper = styled.div`
  position: relative;
  width: 60px;

  > :nth-child(2) {
    position: absolute;
    left: 24px;
    top: 0;
  }
`

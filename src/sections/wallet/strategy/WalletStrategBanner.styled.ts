import styled from "@emotion/styled"

export const SWalletStrategBanner = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;

  height: 89px;

  padding-block: 25px;
  padding-left: 8px;
  padding-right: 40px;

  border: 1px solid #a1a3b866;
  border-radius: 8px;

  background: linear-gradient(
    89.8deg,
    #ff268b 0.05%,
    rgba(196, 6, 85, 0) 74.93%
  );
`

import styled from "@emotion/styled"

export const SFarmIncentive = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px 0;

  & + & {
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
`

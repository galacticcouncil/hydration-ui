import styled from "@emotion/styled"
import { theme } from "theme"

export const SIncentivesContainer = styled.div`
  padding: 15px 20px 0;

  @media ${theme.viewport.gte.sm} {
    margin-left: 30px;
    padding-top: 0;
  }
`

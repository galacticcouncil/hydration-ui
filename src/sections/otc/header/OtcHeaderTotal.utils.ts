import { BN_0 } from "utils/constants"

export const useTotalOffers = () => {
  const data = BN_0
  const isLoading = false
  return { data, isLoading }
}
export const useUserTotalOffers = () => {
  const data = BN_0
  const isLoading = false
  return { data, isLoading }
}

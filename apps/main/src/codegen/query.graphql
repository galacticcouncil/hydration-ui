query OrdersState($offerId: Int!) {
    events(
        where: {
            args_jsonContains: { orderId: $offerId }
            AND: { name_eq: "OTC.Placed" }
        }
    ) {
        name
        args
    }
}
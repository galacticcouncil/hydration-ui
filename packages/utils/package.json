{
  "name": "@galacticcouncil/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc --build",
    "lint": "yarn lint:eslint && yarn lint:ts",
    "lint:ts": "tsc --noEmit -p tsconfig.json",
    "lint:eslint": "eslint src",
    "lint:fix": "eslint src --fix"
  },
  "dependencies": {
    "@noble/hashes": "^1.6.1",
    "@types/uuid": "^10.0.0",
    "buffer": "^6.0.3",
    "uuid": "^11.0.5"
  }
}

import styled from "@emotion/styled"
import { IconButton } from "components/IconButton/IconButton"

export const BackButton = styled(IconButton)`
  color: white;

  transform: rotate(180deg);
`
export const SOmnipoolAssetContainer = styled.div``
